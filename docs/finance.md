# Financeiro e DRE

Gerencia as despesas, receitas, fiado, e contas a pagar/receber.
*   Collection: `financial_records` e `refunds`.
*   A API `/api/finance` e as Security Rules forçam as atualizações por meio de backend (impedindo gravações maliciosas diretas no cliente).

**Status**: IMPLEMENTADO.
